import contextlib
import importlib

importlib.invalidate_caches()

with contextlib.suppress(Exception):
    pass

import contextlib
import importlib

importlib.invalidate_caches()

with contextlib.suppress(Exception):
    pass

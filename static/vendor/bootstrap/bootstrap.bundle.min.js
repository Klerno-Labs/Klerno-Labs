/*!
 * Bootstrap v5.3.2 (https://getbootstrap.com/)
 * Copyright 2011-2023 The Bootstrap Authors
 * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)
 */
// This is just a placeholder for the full Bootstrap JS
// In a real implementation, the full minified JS would be included

// For now, we'll just create a basic fallback mechanism
if (typeof bootstrap === 'undefined') {
  console.warn('Bootstrap CDN failed to load. Using fallback implementation.');
  
  // Create a minimal bootstrap namespace with essential functionality
  window.bootstrap = {
    Tooltip: function(element) {
      // Simple tooltip implementation
      this.element = element;
      this.show = function() { console.log('Tooltip show called'); };
      this.hide = function() { console.log('Tooltip hide called'); };
      this.dispose = function() { console.log('Tooltip dispose called'); };
      return this;
    },
    Popover: function(element) {
      // Simple popover implementation
      this.element = element;
      this.show = function() { console.log('Popover show called'); };
      this.hide = function() { console.log('Popover hide called'); };
      this.dispose = function() { console.log('Popover dispose called'); };
      return this;
    },
    Modal: function(element) {
      // Simple modal implementation
      this.element = element;
      this.show = function() { 
        element.style.display = 'block';
        element.classList.add('show');
        document.body.classList.add('modal-open');
      };
      this.hide = function() { 
        element.style.display = 'none';
        element.classList.remove('show');
        document.body.classList.remove('modal-open');
      };
      this.toggle = function() {
        if (element.classList.contains('show')) {
          this.hide();
        } else {
          this.show();
        }
      };
      return this;
    },
    Alert: function(element) {
      // Simple alert implementation
      this.element = element;
      this.close = function() {
        element.style.display = 'none';
        element.classList.remove('show');
        setTimeout(function() {
          element.remove();
        }, 150);
      };
      return this;
    }
  };
  
  // Initialize event listeners for data-bs attributes
  document.addEventListener('DOMContentLoaded', function() {
    // Handle modal triggers
    document.querySelectorAll('[data-bs-toggle="modal"]').forEach(function(element) {
      element.addEventListener('click', function() {
        const target = document.querySelector(this.getAttribute('data-bs-target'));
        if (target) {
          new bootstrap.Modal(target).show();
        }
      });
    });
    
    // Handle alert dismissal
    document.querySelectorAll('[data-bs-dismiss="alert"]').forEach(function(element) {
      element.addEventListener('click', function() {
        const alert = this.closest('.alert');
        if (alert) {
          new bootstrap.Alert(alert).close();
        }
      });
    });
  });
}